{% set vertx = salt['grains.filter_by']({
    'Debian': {
        'bin_path': '/usr/bin/',
        'install_path': '/opt',
        'bucket': 'deploy_adcade',
        'version': '1.3.1',
    }
}, merge=salt['pillar.get']('vertx:lookup')) %}
